# Server-only Supabase configuration. Never expose the service-role key through
# a NEXT_PUBLIC_ variable.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Used only by the documented psql migration/import commands.
DATABASE_URL=
